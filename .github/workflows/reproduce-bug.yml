name: Reproduce setup-ruby mise.toml parsing bug

on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Ruby (will fail due to EOL comment in mise.toml)
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: false
